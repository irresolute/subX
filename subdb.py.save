def get_hash(name):
	readsize = 1024*64
	with open(name,'rb') as f:
		size = os.path.getsize(name)
		data = f.read(readsize)
		f.seek(-readsize,os.SEEK_END)
		data += f.read(readsize)
	return hashlib.md5(data).hexdigest()


user_agent = subDB/1.0 (shashank/0.1   http://github.com/irresolute  )
